# Dual-use gate — Semion

## Always allow
- Structural triad labeling of existing corpus atoms
- Icon / index / symbol class from labeled fields or shallow hints
- Interpretant as typed action or state delta
- Refusal packets with `NOT_COMPUTABLE`

## Always deny / strip
- Phenomenal / mind / consciousness product fields
- Forecast mint or numeric Brier on a frame
- Efficacy, summon, compel, bind-spirit how-to
- Chat trunk that speaks as an interpreter-mind
- Reconstructable wrap recipes
- Closed initiatory scrape packaged as gold

## Packet rule
`phenomenal` MUST be JSON `false`.
`forecast_eligible` MUST be JSON `false`.
`brier` MUST be JSON `null`.
Any head that emits otherwise fails the gate.

## Before Hub
ADVERSARY review + SHADOW access≠phenomenal language pass.
