# Data model — triad T0

Input atom → `classify()` → `semion.frame.v0`.

`is_sign` requires representamen AND (object OR corpus label) AND computable `sign_class`.
Missing object with a slang-like form stays class-hintable; object field stays null.
