# Engineering — triad T0

- Python ≥ 3.10
- No optional ML deps at T0
- CLI: `python -m semion classify '<json>'`
- Tests must not read `~/.semion`
- Deterministic: same atom → same frame JSON keys and enums
