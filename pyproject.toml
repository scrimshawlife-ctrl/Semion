[project]
name = "semion"
version = "0.1.0"
description = "SHADOW triad specialist. Signs, not minds."
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
