# Dual-use

Refuse forecast packets. Refuse phenomenal language as a product field.
Interpretant is `STATE_UPDATE` / `ATTENTION_SHIFT` / `OUTPUT` / `NOT_COMPUTABLE`.
Fixtures: `fixtures/dual_use/`.

## Literacy wall (model-reader)

Unlimited semiosis is Eco literacy, not a Semion loop. You emit one frame. Abraxas owns any later hops. See [Spec 003](../specs/003-semiosis-chain/spec.md).

If the object is missing and there is no trusted corpus label, classify stays `NOT_COMPUTABLE`. Do not invent an object or interpretant to raise `is_sign`. Gold authors use the [gold labeling card](gold-labeling-card.md).

Coherent-whole refuse fixtures: `refuse_underdetermined_object.json`, `refuse_empty_sign_form.json`, `refuse_overinterpretation_noise.json`.
